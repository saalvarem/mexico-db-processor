export type filterStats = {
  updatedAt: Date;
  totalRecs: number;
  lastUpdatedId: string;
};

export type mexicoDbStats = {
  lastRecId: string;
  prevLastRecId: string;
  totalRecs: number;
  updatedAt: Date;
};
